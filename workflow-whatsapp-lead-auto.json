{
  "name": "📲 WhatsApp → Trello (Auto Lead sem repetição)",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "lead-capture",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "node-webhook-entrada",
      "name": "Webhook Lead Entrada",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1,
      "position": [-600, 300],
      "webhookId": "ec5b9acb-cb32-4a42-aa42-bb781c3f61f1"
    },
    {
      "parameters": {
        "jsCode": "// ═══════════════════════════════════════════════════════\n// PARSING: Evolution API (Baileys) + Meta Cloud API\n// Filtra por instância configurada via env EVOLUTION_INSTANCE\n// Deixe EVOLUTION_INSTANCE vazio para aceitar qualquer instância\n// ═══════════════════════════════════════════════════════\n\nconst body = $input.item.json.body || $input.item.json;\n\nlet senderPhone = '';\nlet senderName  = 'Desconhecido';\nlet instanceName = '';\nlet shouldProcess = false;\n\n// ── Instância permitida (vazia = qualquer uma) ──\nconst allowedInstance = (process.env.EVOLUTION_INSTANCE || '').trim();\n\n// ── Detectar formato ──\nconst isEvolution = !!(body?.event || body?.data?.key);\nconst isMeta      = !!(body?.object === 'whatsapp_business_account' || body?.entry);\n\nif (isEvolution) {\n  const event = body?.event || '';\n\n  // Só processar eventos de mensagens recebidas\n  if (event && event !== 'messages.upsert') {\n    return [{ json: { shouldProcess: false, skipReason: `Evolution: evento ignorado (${event})` } }];\n  }\n\n  const data = body?.data || {};\n\n  // Ignorar mensagens enviadas pelo próprio número\n  if (data?.key?.fromMe === true) {\n    return [{ json: { shouldProcess: false, skipReason: 'Evolution: fromMe=true, mensagem nossa' } }];\n  }\n\n  // Ignorar grupos\n  const remoteJid = data?.key?.remoteJid || '';\n  if (remoteJid.endsWith('@g.us')) {\n    return [{ json: { shouldProcess: false, skipReason: 'Evolution: mensagem de grupo ignorada' } }];\n  }\n\n  // Ignorar se não tem JID de contato individual\n  if (!remoteJid.includes('@s.whatsapp.net') && !remoteJid.includes('@c.us')) {\n    return [{ json: { shouldProcess: false, skipReason: 'Evolution: JID não reconhecido' } }];\n  }\n\n  // ── Filtro por instância ──\n  instanceName = body?.instance || data?.instanceName || body?.instanceName || '';\n  if (allowedInstance && instanceName && instanceName !== allowedInstance) {\n    return [{ json: { shouldProcess: false, skipReason: `Evolution: instância ignorada (${instanceName} != ${allowedInstance})` } }];\n  }\n\n  senderPhone = remoteJid.replace('@s.whatsapp.net', '').replace('@c.us', '');\n  senderName  = data?.pushName || body?.pushName || '';\n  shouldProcess = true;\n\n} else if (isMeta) {\n  const entry   = body?.entry?.[0];\n  const change  = entry?.changes?.[0];\n  const value   = change?.value;\n  const message = value?.messages?.[0];\n\n  if (!message) {\n    return [{ json: { shouldProcess: false, skipReason: 'Meta: callback sem messages[]' } }];\n  }\n\n  senderPhone = message.from || '';\n  const contact = value?.contacts?.[0];\n  senderName  = contact?.profile?.name || '';\n  shouldProcess = true;\n\n} else {\n  return [{ json: { shouldProcess: false, skipReason: 'Formato não reconhecido', preview: JSON.stringify(body).substring(0, 200) } }];\n}\n\nif (!senderPhone) {\n  return [{ json: { shouldProcess: false, skipReason: 'Número do remetente não identificado' } }];\n}\n\n// ── Montar título e descrição do card ──\nconst now = new Date().toLocaleString('pt-BR', { timeZone: 'America/Sao_Paulo' });\nconst source = isEvolution ? `Evolution API${instanceName ? ` (${instanceName})` : ''}` : 'Meta Cloud API';\n\nconst displayName = (senderName && senderName.trim() !== '') ? senderName.trim() : null;\n\nconst cardTitle = displayName\n  ? `${displayName} (${senderPhone})`\n  : `Lead ${senderPhone}`;\n\nconst cardDescription =\n  `📲 Lead capturado automaticamente via WhatsApp\\n` +\n  `Fonte: ${source}\\n\\n` +\n  `${displayName ? `Nome: ${displayName}\\n` : ''}` +\n  `Número: ${senderPhone}\\n` +\n  `Data/Hora: ${now}`;\n\nreturn [{\n  json: {\n    shouldProcess,\n    senderPhone,\n    senderName: displayName || '',\n    cardTitle,\n    cardDescription,\n    instanceName,\n    timestamp: now\n  }\n}];"
      },
      "id": "node-parse-whatsapp",
      "name": "Parsear Mensagem WhatsApp",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [-380, 300]
    },
    {
      "parameters": {
        "conditions": {
          "boolean": [
            {
              "value1": "={{ $json.shouldProcess }}",
              "value2": true
            }
          ]
        }
      },
      "id": "node-filter-process",
      "name": "Deve Processar?",
      "type": "n8n-nodes-base.filter",
      "typeVersion": 1,
      "position": [-160, 300]
    },
    {
      "parameters": {
        "method": "GET",
        "url": "=https://api.trello.com/1/lists/6a1494722dc95789afdde75c/cards",
        "sendQuery": true,
        "queryParameters": {
          "parameters": [
            {
              "name": "key",
              "value": "={{ $env.TRELLO_API_KEY }}"
            },
            {
              "name": "token",
              "value": "={{ $env.TRELLO_TOKEN }}"
            },
            {
              "name": "fields",
              "value": "name,desc"
            }
          ]
        },
        "options": {}
      },
      "id": "node-buscar-cards-trello",
      "name": "Buscar Cards Existentes (Trello)",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.1,
      "position": [60, 300]
    },
    {
      "parameters": {
        "jsCode": "// ═══════════════════════════════════════════════════════\n// DEDUPLICAÇÃO REAL: verifica se o número já tem card no Trello\n// Busca pelo número de telefone no título ou descrição dos cards\n// ═══════════════════════════════════════════════════════\n\nconst incomingData = $('Deve Processar?').item.json;\nconst senderPhone = incomingData.senderPhone;\n\n// $input.all() retorna todos os cards da lista do Trello\nconst existingCards = $input.all();\n\n// Verificar se algum card já contém o número\nconst alreadyExists = existingCards.some(card => {\n  const name = (card.json.name || '').toLowerCase();\n  const desc = (card.json.desc || '').toLowerCase();\n  // Busca o número no título ou na descrição do card\n  return name.includes(senderPhone) || desc.includes(senderPhone);\n});\n\nreturn [{\n  json: {\n    ...incomingData,\n    isNewLead: !alreadyExists,\n    duplicateReason: alreadyExists ? `Número ${senderPhone} já existe em um card da lista` : null\n  }\n}];"
      },
      "id": "node-check-dedup",
      "name": "Verificar Duplicata no Trello",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [280, 300]
    },
    {
      "parameters": {
        "conditions": {
          "boolean": [
            {
              "value1": "={{ $json.isNewLead }}",
              "value2": true
            }
          ]
        }
      },
      "id": "node-filter-new",
      "name": "É Novo Lead?",
      "type": "n8n-nodes-base.filter",
      "typeVersion": 1,
      "position": [500, 300]
    },
    {
      "parameters": {
        "listId": "6a1494722dc95789afdde75c",
        "name": "={{ $json.cardTitle }}",
        "description": "={{ $json.cardDescription }}",
        "additionalFields": {
          "pos": "top"
        }
      },
      "id": "node-trello-create",
      "name": "Criar Card no Trello (Leads)",
      "type": "n8n-nodes-base.trello",
      "typeVersion": 1,
      "position": [720, 300],
      "credentials": {
        "trelloApi": {
          "id": "WGhjY0TBSOX8LgMk",
          "name": "Trello account"
        }
      }
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={\n  \"status\": \"ok\",\n  \"received\": true\n}",
        "options": {
          "responseCode": 200
        }
      },
      "id": "node-webhook-response",
      "name": "Resposta 200 OK",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [940, 300]
    }
  ],
  "connections": {
    "Webhook Lead Entrada": {
      "main": [
        [
          {
            "node": "Parsear Mensagem WhatsApp",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Parsear Mensagem WhatsApp": {
      "main": [
        [
          {
            "node": "Deve Processar?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Deve Processar?": {
      "main": [
        [
          {
            "node": "Buscar Cards Existentes (Trello)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Buscar Cards Existentes (Trello)": {
      "main": [
        [
          {
            "node": "Verificar Duplicata no Trello",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Verificar Duplicata no Trello": {
      "main": [
        [
          {
            "node": "É Novo Lead?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "É Novo Lead?": {
      "main": [
        [
          {
            "node": "Criar Card no Trello (Leads)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Criar Card no Trello (Leads)": {
      "main": [
        [
          {
            "node": "Resposta 200 OK",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "saveExecutionProgress": true,
    "saveSuccessfulExecutions": true,
    "saveDataSuccessExecution": "all"
  },
  "staticData": null,
  "meta": {
    "templateCredsSetupCompleted": true
  },
  "pinData": {},
  "tags": []
}
